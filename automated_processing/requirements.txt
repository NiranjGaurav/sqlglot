# Core dependencies for hybrid prefork + Redis architecture
celery[redis]==5.3.4
redis==4.6.0
pyarrow==17.0.0
numpy==1.24.3
fastapi==0.104.1
uvicorn==0.24.0
tqdm==4.66.1
# PyIceberg with compatible versions (for storage service only)
pyiceberg==0.7.0
boto3==1.34.0
botocore==1.34.0
s3fs==2023.12.0
fsspec==2023.12.0
# Removed unused packages:
# eventlet==0.33.3 (not needed - using prefork)
# gevent==23.9.1 (not needed - using prefork)
# sqlalchemy==2.0.23 (not used)
# urllib3==2.2.3 (included with other packages)